export default {
  testEnvironment: 'node',
  verbose: false,
  collectCoverage: true,
  coverageDirectory: 'coverage',
  testMatch: ['**/src/**/*.spec.js'],
  transform: {},
  reporters: ['default'],
};
